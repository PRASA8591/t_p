{
  "name": "mihcm-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --exec node ./src/index.js",
    "start": "node ./dist/index.js",
    "build": "echo \"No build step configured for server\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.1.4",
    "express": "^4.19.4",
    "mongoose": "^7.7.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.4"
  }
}
